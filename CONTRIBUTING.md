# Contributing

Use Node.js 20 or newer. Run `npm ci`, then `npm run test:all`. Changes to the
pinned Playwright version must be isolated because Chromium upgrades can change
every baseline. Open an issue before changing the public configuration schema.
